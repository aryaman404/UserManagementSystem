<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <link rel="stylesheet" href="/stylesheet/style.css" />
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body>
    <div class="w-full h-full bg-zinc-900 p-10 text-white">
      <div class="mb-3">
        <a class="text-blue-500" href="/">Back to Home</a>
      </div>
      <h1 class="text-3xl mb-5 tracking-tighter">All Users</h1>
      <div class="users flex gap-3 fle-wrap">
        <% if(user.length>0){ %> <% user.forEach(function (user) {%>
        <div class="user max-w-56 p-4 bg-zinc-600 rounded-lg">
          <div class="w-full h-52 rounded-lg bg-zinc-800">
            <img
              class="w-full h-full object-cover object-top"
              src="<%= user.image%>"
            />
          </div>
          <h1 class="text-xl mt-3 tracking-tighter"><%= user.name%></h1>
          <h5 class="text-zinc-400"><%= user.email %></h5>
          <div class="flex gap-3 mt-4 justify-between">
            <a class="text-zinc-300" href="/edit/<%=user._id%>">Edit User</a>
            <a class="text-red-500" href="/delete/<%=user._id%>">Delete User</a>
          </div>
        </div>
        <% }) %> <%}else {%>
        <h3 class="text-zinc-500">No Users Yet</h3>
        <%}%>
      </div>
    </div>
  </body>
</html>
